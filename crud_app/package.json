{
    "name": "your-app-name",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "build": "node ./esbuild.config.js"
    },
    "devDependencies": {
        "esbuild": "^latest"
    },
    "dependencies": {
        "firebase": "^9.0.0"
    }
}
